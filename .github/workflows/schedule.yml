on:
  schedule:
    - cron: '*/2 * * * *'
jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - uses: yoksulkapici12/barsoom@0.0.6
      - name: Echo current time
        run: echo "The current server time is $(date)"
